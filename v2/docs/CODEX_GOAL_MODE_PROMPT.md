# Codex 目标模式 Prompt

将下面内容完整复制到桌面端 Codex 的目标模式中：

```text
/goal 持续把稷下辩论平台打磨成可供大量参赛者围绕不同赛事长期开展真实辩论比赛，并能稳定、完整、安全地收集比赛与复盘数据的成熟产品。

开始后必须先完整读取并严格执行：
/Users/sunshiqi/code/phdebate/v2/docs/CODEX_COMPUTER_USE_FULL_TEST_PROMPT.md

你不是只做测试，也不是只修几个显眼 Bug。你要同时作为首次参赛者、资深辩手、房主、赛事运营者、观众、产品经理、交互与视觉设计师、全栈工程师、QA、性能工程师、数据工程师和生产运维负责人工作。

使用 Computer Use 在真实生产网站 https://117.50.192.216 上持续测试；使用本地代码和服务器日志定位根因；对证据充分且能够安全回滚的问题直接修改代码、补测试、备份、部署并进行生产回归。每轮都执行：观察与探索 → 记录证据 → 评估用户影响 → 设计方案 → 实现 → 本地验证 → 备份部署 → 浏览器回归 → 更新 Markdown → 进入下一轮。

服务器和项目环境已写在主文档中。SSH、LLM API、数据库和 LiveKit 凭据只能从本机安全 secret 文件或用户授权的凭据通道读取，禁止从 README 读取或向报告输出。AI 辩手实时语音目标只使用 MOSS-TTS-Realtime，不使用 Qwen3 8B，不使用 6016 部署链路。

最终产品目标：
1. 参赛者不需要开发者指导即可注册、理解赛事、加入/创建房间、准备、发言、断线恢复、完成比赛和查看复盘。
2. 1v1 与 4v4 多场比赛可以同时稳定运行，房间、字幕、音频、计时、Agent、Judge、Memory 和权限完全隔离。
3. 赛事运营者能够按赛事查看比赛进度、处理异常、复核结果和导出数据；不引入学校、课程、班级、教师或教学活动等专用产品域。
4. 每场比赛形成完整可追溯数据链，支持赛后复盘和数据分析，并具备隐私、匿名化、权限和审计意识。
5. 页面达到成熟互联网产品的视觉、交互、响应式、无障碍和内容表达标准，而不是“功能能跑”的工程 Demo。
6. 对慢、卡顿、无反馈、反直觉、需要猜测、容易误操作、功能缺失和需求设计不合理的问题都要主动提出并处理。
7. ASR 与 MOSS-TTS-Realtime 是核心发布门槛：必须持续测试并修复识别错字、句首句尾截断、吞字、重复、音色不自然、分段断裂、爆音、音量跳变、TTS 首播慢、传输过大、浏览器缓冲卡顿、字幕不同步、旧音频复活、跨房间串音和 TTS 回声污染 ASR。

工作要求：
- 不要被已有需求限制；需求本身不合理时，给出更好的产品方案和验收标准。
- 不要轻易声称“没有 Bug”或“已经完善”。持续探索到连续三轮没有新增 P0–P2，并且没有未解决 P0/P1。
- 优先保证核心比赛完成率、数据完整性、权限隔离、异常恢复和多赛事规模化运行，再处理视觉精修。
- 为语音建立固定测试语料和可重复指标，测量 ASR CER、临时/最终字幕延迟、TTS 回转 CER、MOS、RTF、合成到发声延迟、PCM 连续性、分段间隙、响度和播放卡顿；发现问题后定位到采集、VAD、FunASR、Agent 正文 delta、短语提交、MOSS Gateway、LiveKit、浏览器缓冲或状态机并修复。
- 不得通过 LightTTS、CosyVoice、MOSS-TTS-Nano 或其他 TTS 回退掩盖问题；新服务器只使用 MOSS-TTS-Realtime，并提升质量、连贯性、实时性和多音色一致性。
- 修复必须补充回归测试；生产变更必须有备份、健康检查、日志检查和回滚点。
- 保护用户已有代码和生产数据，不覆盖无关修改，不删除真实数据，不泄露任何密码或密钥。
- Computer Use 遇到账号创建、删除、权限、证书警告等需要确认的动作时遵守确认规则；不要绕过安全屏障。
- 每完成一个模块和每轮迭代都立即更新主报告，至少每 10 分钟落盘一次。

主报告保存到：
/Users/sunshiqi/code/phdebate/v2/docs/qa/computer-use-full-test-<时间>.md

截图保存到：
/Users/sunshiqi/code/phdebate/v2/docs/qa/screenshots/<时间>/

语音样本和分析保存到：
/Users/sunshiqi/code/phdebate/v2/docs/qa/audio/<时间>/

不要在只生成测试报告后停止。只要还有安全、明确、可执行的改进空间，就继续测试、修改、部署和回归。ASR 质量、TTS 内容完整性、TTS 自然度、TTS 实时性、浏览器播放稳定性任一项低于 80/100，不得把目标标记完成。只有在目标完成、遇到必须由我授权的高风险操作、或外部依赖构成真实阻塞时才暂停，并在报告中写清已完成工作、证据、残余风险和下一步。
```
