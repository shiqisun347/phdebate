1. 开场、换阶段、应该有主持人预设语音播报。

2. 右边应该有一个文字记录的按钮，点击以后，可以弹出一个文字记录的面板，显示当前阶段的文字记录。可以修改文字记录(针对每个发言的修改)，修改后可以保存。文字记录面板应该支持多人同时编辑和查看。参考 github 相关项目中成熟的代码改进，不要自己从头写

3. 现在的文字字幕是不合理的，不要使用"发言正在生成或进行中",最后出现了1s左右的全部内容，用户也看不完就跳过了。发言应该参考字幕的实现和形式，逐句显示。ASR发言也是以字幕的形式。参考 github 相关项目中成熟的代码改进，不要自己从头写

4. 自我辩论阶段，单次发言时间不超过 30s。每次发言之间有3s的间隔。该阶段有一个申请发言的按钮，点击以后再次点击是取消。可点击的时间段是，对方辩手发言开始到对方辩手发言结束后3s以内。如果超过这个时间段，申请发言按钮变为不可点击状态。如果所有的辩手都没有申请发言，则默认由agent发言（优先由想要发言的辩手发言，不管要不要发言，如果在对方辩手发言结束时，还没有人类或者agent申请发言，就立马判断 agent是否应该发言，如果是立马生成发言内容用于预备;其他情况你也可以适当的设置一些策略，用于加速，避免自由辩等待过长的时间）。

5. 如果多个辩手同时申请发言，则优先由最先申请的辩手发言，其他辩手需要等待下一轮机会。申请发言时，应该可以看到当前的申请发言队列，显示每个辩手的申请时间和顺序。比如在选手信息框旁边有一个小的举手图标且有一个小的数字表示其该用户申请发言和举手顺序。

6. 自我辩论阶段，agent需要额外的判断机制，决定自己是否应该发言。为保证效率，可以同时调用判断和生成发言内容的接口，判断结果出来后，如果agent不需要发言，则丢弃生成的内容；如果agent需要发言，则备用。

7. 实现必要的性能优化，比如下一阶段是由agent发言时，可以在当前阶段的文字稿已经固定以后，tts 发声过程中，即可提前生成发言内容和按照队列tts。其他情况，你也可以适当的设置一些策略，用于预加载和加速。

8. 语音播放速度应该是最初的 1.1倍，当前我在比赛中，tts 语音有杂音、撕裂音、卡顿的感觉。但是我在赛后播放录音，效果就挺好的。
