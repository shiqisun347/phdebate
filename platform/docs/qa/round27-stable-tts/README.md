# Round 27：生产单场稳定语音验收

验收时间：2026-07-22（Asia/Shanghai）  
生产房间：`831401`（一次性测试数据，验收后清理）

## 结论

- 1v1 双 AI 比赛完整经过正方立论、反方立论、AI 裁判并生成赛果。
- 两段正式语音均先生成完整 WAV，再以 1.1 倍速通过同一条 LiveKit 音轨连续发布。
- 浏览器收包期间 `packetsLost=0`，最大 WebRTC jitter 为 `19ms`。
- `concealedSamples` 只在接入初期从 0 增至 4800，之后两段长语音播放期间没有继续增长；未再出现此前随播放持续累积的 underrun。
- 正方音频 59.501 秒，反方音频 54.257 秒，比赛随后正常进入裁判并完成。

## 仍需修复

- 当前本机 MOSS 生成完整语音耗时仍高于音频时长。首段准备 111.084 秒，不能满足“Agent 首字后 3 秒内出声”。完整 WAV 模式解决了卡顿、撕裂和中途结束，但属于质量优先的安全路径，不是最终低延迟路径。
- 第二段预生成任务已在第一段播放期间启动，但正式阶段开始时被通用预取清理逻辑取消，导致重新生成，准备耗时 101.336 秒。代码已修复为等待并采用同房间有效预取，需部署后再做一次生产回归。
- 浏览器响度检测在自然标点停顿处出现约 0.1–0.23 秒的静音切换；网络统计没有对应丢包或隐藏样本增长，当前判断为语音本身停顿，而非传输撕裂。

## 证据

- [完成后的结果页截图](./room-831401-completed.png)
- 正方：`audio.rtc.started` 16:59:35.561 UTC，`speech.audio.ready` 17:00:35.081 UTC。
- 反方：`audio.rtc.started` 17:02:17.535 UTC，`speech.audio.ready` 17:03:11.797 UTC。
- 浏览器稳定播放期间 Opus 码率约 30–64kbps；两段播放均无丢包。

> 注意：本轮证明“完整音频连续发布”可以消除现场 underrun；低于 3 秒的首声目标仍需要速度足够的真双向云 TTS，或在比赛开始前完成第一轮语音准备。
