# Round 8 生产部署后复测

日期：2026-07-20  
环境：`https://117.50.192.216`

## 发布结果

- API primary/secondary、Web、Engine 和 Worker 已切换到
  `round8-recovery-product-20260720`。
- PostgreSQL schema 为 `0025_speech_result_pagination`。
- 公共 readiness 全绿；检查包含数据库、Redis、Engine、Worker、MOSS、FunASR、存储和备份。
- 本轮没有修改 DebateStage、LiveKit、AudioWorklet、PCM 播放队列、MOSS 模型、音色或语音参数。

## 浏览器回归

使用 agent-browser 在真实生产页面复测：

1. 桌面端从已滚动的首页进入 4v4 赛事详情，页面正确回到顶部，主标题层级正确。
2. 390×844 手机视口下，赛事标签保持单行并可横向滚动；每个触控项高度为 50px，页面没有横向溢出。
3. 匿名访问历史赛果时只显示“返回比赛现场”，不再错误引导到需要登录的个人中心。
4. 首页和赛事详情都不再展示长期暂停的房间 `551958`，赛事公开房间数均为 0。

测试期间生成了桌面赛事顶部、手机赛事标签和匿名结果页三张截图；截图仅用于当轮人工核对，
不进入公开 GitHub 源码备份，结论保留在本报告中。

## QA 数据收口

- 房间 `968296` 已取消并标记为测试数据。
- 两个 Round 8 QA 账号已禁用并标记为测试账号。
- 两个账号的登录会话均已撤销；历史记录保留用于审计，不直接删除关系数据。

## 备份与恢复验证

- 平台 DB：`auto-20260720T070147Z.dump`，临时数据库恢复成功，24 张表计数一致。
- Agent DB：`agent-20260720T071857Z.dump`，临时数据库恢复成功，13 张表存在。
- 数据卷：`20260720T070152Z-data-volumes.tar.gz`，隔离解包后 380 个文件、393862602 字节逐项校验通过。
- 可靠音频基线仍为 82 个文件，指纹保持
  `3219138b22878e766c94b9a1fa0e422211a6f27dd74afbbb93a8e45f7e295285`。

## 已知边界

- 主机本轮发生过一次整机重启；本机无持久 journal，无法证明外部重启原因。
- 已将 Nginx 与 MOSS 的开机恢复配置纳入 GitHub 源码并安装到 Supervisor，但没有为验证而再次主动重启生产主机。
- 生产磁盘剩余约 12GB；release 清理工具已提供 dry-run 和保护规则，本轮没有删除可靠语音相关历史 release。
