# Round 57 最终修复与生产验收

- 生产地址：`https://117.50.192.216`
- 最终发布：`round57c-caption-playback-20260723`
- Source tree：`91492a65782c4839f0d79a138d8b2a570e11e53082afa2fe2e6dbe5414df360f`
- 验收日期：2026-07-23（Asia/Shanghai）

## 本轮完成

1. 永久移除 AI 自动接管真人席位。
2. 真人断线未满 60 秒时保留原席位；达到 60 秒后暂停整场。
3. 真人重连不会自动恢复比赛，必须由房主或管理员显式继续。
4. 真人发言被断线暂停打断后，继续比赛仍等待本人重新开始发言，计时不会偷跑。
5. 4v4 真人与永久 AI 混合阵营中，真人全部离线时，AI 队友不能在宽限期内顶替发言或提前生成发言。
6. 自由辩论等待真人点击发言时，总计时、单轮计时和当前阵营全部冻结。
7. 预生成 Agent 发言也会产生参与者可见的逐句字幕；字幕按照连续音轨播放进度推进，不一次显示完整发言。
8. 匿名观众和登录观众仍无法读取实时字幕、完整发言或赛后文字记录。
9. 结果页刷新时同步保存已加载的历史分页，避免总结发言在刷新竞态中消失。
10. 控制菜单身份、移动端自由辩论底栏、举手窗口到时状态和观战页面说明完成修正。

## 自动化结果

- API：`576 passed, 1 xfailed`；Ruff 通过。
- Web：`362 passed`，无跳过测试；生产构建通过。
- ESLint：0 error，12 个既有 warning。
- 视觉：1440×900、390×844 的 lobby、control、debate、free debate、watch 均通过布局检查。

## 生产断言

- 房间 `372389`：真人断线 60 秒后状态为 `paused`，席位仍为 `human`，没有 `seat.ai_substituted`，重连后房主手动恢复成功。
- 房间 `264798`：自由辩论等待 5 秒前后均为总计时 `240`、单轮 `30`、正方轮次，未发生空轮换。
- 房间 `507081`：预生成 AI 发言成功产生参与者字幕；公开投影字幕数为 0，AI 完整文本为空。
- 房间 `341598`：字幕段统一为 `estimated_playback`，偏移从 `0ms` 递增，后续短句不会在首音时提前显示。
- 最终 `/api/live-rooms` 为空，没有遗留测试房间。
- `/api/health/ready` 全部通过：PostgreSQL、Redis、Engine、Worker、MOSS、FunASR、存储和备份均正常。

## 对旧黑盒报告的处理

`round57-browser-fullflow-20260723.md` 测试的是 Round 55 生产包。其中自由辩论计时、AI 字幕、旧文字弹窗和断线暂停文案问题已在本发布修复。结果页缺失问题进一步定位为“加载旧分页后立即刷新”的状态竞态，本发布也已修复并增加回归测试。
