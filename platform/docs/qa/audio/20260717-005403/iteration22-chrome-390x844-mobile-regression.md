# Iteration 22：Chrome 390×844 移动端生产回归

## 范围与边界

- 时间：2026-07-17 18:44–18:58 CST。
- 浏览器：Google Chrome；未使用 AdsPower/SunBrowser，Safari 的系统密码锁未绕过。
- 目标：生产站 `https://117.50.218.251` 的学生首页、移动导航、1v1 创建窗口、个人中心、辩论舞台、比赛操作面板和退出保护。
- 视口：最终页面证据为 `window.innerWidth=390`、`innerHeight=844`、`devicePixelRatio=1`、`visualViewport=390×844`，文档 `clientWidth/clientHeight=390×844` 且无横向溢出。TC-995–998 是视口切换稳定前保存的 375×812 内容截图；TC-999–1001 是精确 390×844 截图。
- 安全边界：只测试和保存证据；未修改代码、未部署、未创建房间、未接管设备，也未点击重试、暂停、恢复、提前结束或确认退出。

## 结果

| TC | 状态 | 结果与证据 |
|---|---|---|
| TC-995 | PASS | 首页在窄屏下无横向溢出，正式赛/训练赛创建入口可见且主 CTA 高 46px。截图：`TC-995-Iteration22-Chrome-390x844-首页与学生创建入口.png`。 |
| TC-996 | FAIL / P2 | 移动导航可展开且所有入口可达，但菜单 36×36、账号 90×37、退出 35×35、展开导航 317×39，未达到项目 44×44 CSS px 触控目标。截图：`TC-996-Iteration22-Chrome-390x844-移动导航与触控目标.png`。 |
| TC-997 | PASS | 1v1 创建窗口完整落在视口内，无横向溢出；选择卡、席位按钮和提交按钮可达。关闭按钮约 42×42，作为 TC-996 同一 P2 的次要实例。截图：`TC-997-Iteration22-Chrome-390x844-1v1创建窗口.png`。 |
| TC-998 | PASS | `/me` 无横向溢出，进行中比赛卡和“继续比赛”入口可见、可达。截图：`TC-998-Iteration22-Chrome-390x844-个人中心与继续比赛.png`。 |
| TC-999 | PASS | `/rooms/278571/debate` 在 390×844 下无横向溢出；双方席位、暂停告警、权威剩余时间、多设备只读提示和底部控制均可见。核心退出/声音/全屏/操作按钮均 44×44。截图：`TC-999-Iteration22-Chrome-390x844-辩论舞台与多设备只读.png`。 |
| TC-1000 | PASS | “比赛操作”移动面板在 failure + other-device 状态下完整可见；提前结束、退出页面均 320×44。面板关闭约 27.7×42、设备接管 147×38，连同异常重试 328×40 计入 TC-996 的系统性 P2。未点击危险动作。截图：`TC-1000-Iteration22-Chrome-390x844-比赛操作面板.png`。 |
| TC-1001 | PASS | “退出比赛页面”打开二次确认，明确比赛继续、席位保留和 AI 可能接替；取消与确认按钮均高 44px。只点击取消，未退出或释放席位。截图：`TC-1001-Iteration22-Chrome-390x844-退出比赛页面确认.png`。 |

## 缺陷结论

新增一个系统性 `P2 / MOBILE / ACCESSIBILITY`：在 Chrome 窄屏视口下，核心创建、继续比赛、舞台状态和主要比赛操作均可达，但全局导航及少数恢复控件未达到项目 44×44 CSS px 触控目标，会增加单手操作和精细动作困难用户的误触风险。当前没有复现目标重叠、误触后无法恢复或流程阻断，因此不升级为 P1。

建议统一将移动菜单、账号/退出、创建窗关闭、异常重试、面板关闭和设备接管补齐到至少 44×44，并补测 320/375/390 宽、横屏、200% 文本缩放、长姓名与真实触控单击。

## 浏览器与生产只读对账

- Chrome warning/error console：0。
- 2026-07-17 18:58:24 CST 只读复核：房 278571 仍为 `paused / remaining 172 / stage_deadline=NULL / match=running / active_speech=false`。
- 本轮仅新增 seq 107 `presence.connected`（18:52:39）与 seq 108 `presence.disconnected`（18:57:45）；没有 `control.pause/resume/retry/terminate`、`seat.control_acquired/taken_over` 或其他控制写入。
- 测试结束后 `aff_3.connected=false`，比赛状态未变化。

## 结论

390×844 学生核心页面移动布局通过；新增一个不阻断流程的 P2 触控尺寸问题。该结果不关闭生产真人 ASR、Safari、流式音频 P0 或 2–3 场真实并发容量缺口，整体发布结论仍为“不可发布”。
