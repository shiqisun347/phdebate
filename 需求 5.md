其次调整战队信息框大小，当前太大，下面触底了，同时调整 正反方立场，比如“提问思维“的显著度，当前不够显著；比赛控台页面的 比赛过程阶段，下面应该增加 “agent 回复流模块“，包含发送 agent 请求后，返回结果的实时、友好化展示，信息包括一些实时状态信息和流式实时回复 ，增加“TTS 实时模块“，包含实时生成进度，大屏幕实时播放进度条，可以用一个进度条的不同锚点表示，以及一些实时状态信息等 

发言完毕页面，中间的内容 应该只有两部分  上面是 当前阶段，比如反方一辩立论  然后下面是 反方一辩 穷理 发言完毕 ，其他内容都不需要了，比如等待主持确认
反方一辩 · 启问 发言完毕，等待主持确认进入下一环节。任何环节都不应该出现类似的内容，给用户无感的控制


TTS语音流当前暂时不需要修改，但是文本流存在问题，比如在发音过程中，其实回复文本早就已经收到了，那么应该及时实时的维护到 实时辩论、历史会话中，


当前的 tts 容错太低了，刚才遇到了 tts 发音过程中卡顿停止，就不能再继续了。我认为应该从两方面改进该问题，1. 

比赛管理中的“比赛名称“，并没有同步大屏幕的左上角，比赛管理的信息里面是有比赛 id ； 当前的 ai 发言的步骤，上面的动画效果还不够明显；“AI思考中“需要更明显 

 生成完成后开始播报并计时 这个是无需显示的，其次，放大

1. 当前我得感知至少有5秒以上的延迟，我也不知道具体的原因，我认为至少应该在 2S 以内，但是不能破坏其他功能、不破坏容错机制，不破坏连贯性。

自由辩论阶段，规则是对于发言的一方，如果人类辩手没有在两秒以内点击开始发言或者全部点击跳过（需要在合适的位置进入自由辩论阶段增加一个跳过按钮，该按钮只能在自由辩论，下一轮有发言权限时才能点击，在对方发言的当前轮开始到对方发言完两秒内可以点击），那么由 AI 发言，当前的辩手端网页功能需要进行修改，也需要对其他需要进行适配的地方进行修改